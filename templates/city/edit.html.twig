{% extends 'base.html.twig' %}

{% block title %}Modifier City{% endblock %}
{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/main.css') }}">
<link rel="stylesheet" href="{{ asset('css/btn.css') }}">
<link rel="stylesheet" href="{{ asset('css/city.css') }}">
<link rel="stylesheet" href="{{ asset('css/home.css') }}">
{% endblock %}

{% block body %}

<div class="container">
    <h1>Modifier la ville</h1>

    <div id="btn-city">
        {{ include('city/_form.html.twig', {'button_label': 'Modifier'}) }}
        {{ include('city/_delete_form.html.twig') }}
        <a class="btn btn-outline-secondary" style="font-weight: bold;" href="{{ path('app_city_index') }}">back to
            list</a>
    </div>
</div>
{% endblock %}