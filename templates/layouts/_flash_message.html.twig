{# {% for type, messages in app.flashes(['success', 'danger', 'info', 'warning']) %}
{% for message in messages %}
<div class="toast-container position-fixed bottom-50 end-50 p-3" style="z-index: 11">
	<div class="alert alert-{{ type }} alert-dismissible fade show">
		{{ message }}
	</div>
</div>
{% endfor %}
{% endfor %}  #}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/main.css') }}">
{% endblock %}

{% for type, messages in app.flashes(['info', 'success', 'warning', 'danger']) %}
	{% for message in messages %}

		<div class="alert alert-{{ type }} position-fixed bottom-0 start-0 w-25 m-4">
			<div class="toast-header">

				<strong class="me-auto">Soin de Soi</strong>
				<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
			</div>
			<div class="toast-body container">
				{{ message }}
			</div>
		</div>

	{% endfor %}
{% endfor %}
