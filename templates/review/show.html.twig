{% extends 'base.html.twig' %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/main.css') }}">
<link rel="stylesheet" href="{{ asset('css/btn.css') }}">
{% endblock %}

{% block title %}Voir le commentaire{% endblock %}

{% block body %}
    <h1>{{ review.product.name }}</h1>

    <table class="table table-striped container">
        <tbody>
            <tr>
                <th>Auteur</th>
                <td>{{ review.user.firstName }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ review.comment }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ review.note }}</td>
            </tr>
        </tbody>
    </table>


     <a class="btn btn-outline-secondary" style="display: block; margin: 0 auto; width: 200px;" href="{{ path('app_review_index', {'slug': review.product.slug}) }}">retour à la liste</a>

  {% if review.id %}
    <a class="btn btn-outline-warning" style="display: block; margin: 0 auto; width: 200px;" href="{{ path('app_review_edit', {'id': review.id}) }}">modifier</a>
{% endif %}
     <div id="btnReview">
    {{ include('review/_delete_form.html.twig') }}
    </div>

{% endblock %}
