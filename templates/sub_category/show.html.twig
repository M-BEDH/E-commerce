{% extends 'base.html.twig' %}

{% block title %}SubCategory{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/main.css') }}">
    <link rel="stylesheet" href="{{ asset('css/category.css') }}">
{% endblock %}


{% block body %}
    <h1>Sous catégories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_category.name }}</td>
            </tr>
        </tbody>
    </table>

<div id="actions">
    <a class="btn btn-outline-secondary" href="{{ path('app_sub_category_subCategory') }}">back to list</a>

    <a class="btn btn-warning" href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}">Modifier</a>

    {{ include('sub_category/_delete_form.html.twig') }}

    </div>
{% endblock %}
